import type { ReactNode } from "react";
export const metadata = { title: "Settings — Patchwork OS" };
export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
