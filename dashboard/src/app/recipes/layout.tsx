import type { ReactNode } from "react";
export const metadata = { title: "Recipes — Patchwork OS" };
export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
