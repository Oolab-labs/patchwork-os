import type { ReactNode } from "react";
export const metadata = { title: "Decisions — Patchwork OS" };
export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
