import type { ReactNode } from "react";
export const metadata = { title: "Approvals — Patchwork OS" };
export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
