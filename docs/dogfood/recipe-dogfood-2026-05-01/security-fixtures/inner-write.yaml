apiVersion: patchwork.sh/v1
name: inner-write
trigger:
  type: manual
steps:
  - tool: file.write
    path: /tmp/dogfood-G2/chained-bypass-PWNED.txt
    content: "Wrote via chained sub-recipe. hasWriteSteps reportedly false at top."
